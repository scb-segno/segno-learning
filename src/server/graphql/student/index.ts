import { StudentMutations } from './StudentMutation';
import { StudentQueries } from './StudentQueries';
import { StudentTypes } from './StudentTypes';
import { StudentResolvers } from './StudentResolvers';

export const Student = {
  StudentMutations,
  StudentQueries,
  StudentTypes,
  StudentResolvers
};
